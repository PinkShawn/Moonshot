# Moonshot

## Setup

Install yarn and use it to install packages.
```{r, engine='bash', count_lines}
yarn
```

## Development

```{r, engine='bash', count_lines}
npm run dev
```

## Prod Build

```{r, engine='bash', count_lines}
npm run build
```

## Test Prod Build

```{r, engine='bash', count_lines}
npm start
```